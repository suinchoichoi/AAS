/*  Test prepared by compliance@opcfoundation.org
    Description: Read raw history while the server is not collecting data. */

function deleteValueTest() {
    notImplemented( "This test case is intended to be executed manually." );
    return( true );
}

Test.Execute( { Procedure: deleteValueTest } );